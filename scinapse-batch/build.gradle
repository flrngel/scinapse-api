plugins {
    id 'org.springframework.boot'
}

springBoot {
    executable = true
}

dependencies {
    compile project(':scinapse-domain')

    compile 'org.springframework.boot:spring-boot-starter-batch'
    compile 'com.h2database:h2'

    compile 'com.mashape.unirest:unirest-java:1.4.9'
}
